syntax = "proto3";

import "Enum.proto";
import "Structure.proto";

option optimize_for = SPEED;

message LOGIN_REQ
{
    PROTOCOL   id                  = 1 ;
    string var_id                       = 2;
    string var_passwd                = 3;
}