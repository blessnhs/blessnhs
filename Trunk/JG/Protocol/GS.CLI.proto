syntax = "proto3";

import "Enum.proto";
import "Structure.proto";

option optimize_for = SPEED;

package PROTOCOL_GS_TO_CLI; // gameserver -> client

message LOGIN_RES
{
    PROTOCOL   id                  = 1;

    ErrorCode    var_code          = 2;
   
}
