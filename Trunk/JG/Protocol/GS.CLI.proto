syntax = "proto3";

import "Enum.proto";
import "Structure.proto";

option optimize_for = SPEED;

message LOGIN_RES
{
    PROTOCOL   id                  = 1;

    ErrorCode    var_code          = 2;
   
}
